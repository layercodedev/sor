{
  "name": "@layercode/sor",
  "version": "0.1.1",
  "description": "CLI for SOR - SQLite on Rest\n\nSetup: Configure your backend URL and API key before using:\n  $ sor config set url https://your-worker.your-subdomain.workers.dev\n  $ sor config set key your-api-key\n\nGet started:\n  $ sor init              # Add project instructions for AI assistants\n  $ sor db create mydb    # Create a database\n  $ sor sql mydb \"SELECT 1\" # Execute SQL",
  "type": "module",
  "bin": {
    "sor": "./bin/run.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/bin",
    "/dist",
    "/oclif.manifest.json"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "postpack": "rm -f oclif.manifest.json",
    "prepack": "rm -rf dist && tsc && oclif manifest"
  },
  "oclif": {
    "bin": "sor",
    "dirname": "sor",
    "commands": "./dist/commands",
    "topicSeparator": " "
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/layercodedev/sor.git"
  },
  "homepage": "https://github.com/layercodedev/sor",
  "license": "MIT",
  "keywords": [
    "sqlite",
    "cloudflare",
    "durable-objects",
    "database",
    "cli"
  ],
  "dependencies": {
    "@oclif/core": "^4.2.1",
    "@oclif/plugin-help": "^6.2.24"
  },
  "devDependencies": {
    "@oclif/test": "^4.1.15",
    "@types/node": "^22.10.2",
    "oclif": "^4.17.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
